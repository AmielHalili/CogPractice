name: Deploy

on:
  push:
    branches: [BankAppWithJWT]
  workflow_dispatch:

jobs:
  deploy-backend:
    name: Deploy backend to Lambda
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install production dependencies
        run: npm install --omit=dev

      - name: Build deployment zip
        run: zip -r lambda-deploy.zip lambda.js app.js routes services models middleware node_modules

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AKIAUCBERGMA3IIY5TOW}}
          aws-secret-access-key: ${{ secrets.ylbyxBDSiDzzBtKxWmGgdqd2h8kw8ypdXA7DSfU5 }}
          aws-region: ${{ vars.us-east-1 }}

      - name: Update Lambda function code
        run: aws lambda update-function-code --function-name ${{ vars.student-amielhalili-bank-api }} --zip-file fileb://lambda-deploy.zip

  deploy-frontend:
    name: Deploy frontend to S3
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install

      - name: Build
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AKIAUCBERGMA3IIY5TOW}}
          aws-secret-access-key: ${{ secrets.ylbyxBDSiDzzBtKxWmGgdqd2h8kw8ypdXA7DSfU5 }}
          aws-region: ${{ vars.us-east-1 }}

      - name: Sync to S3
        run: aws s3 sync dist/ s3://${{ vars.student-amielhalili-bankuploads }} --delete
